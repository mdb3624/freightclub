---
name: commit
description: Stage, summarize, and commit current changes
---
1. Run `git status` and `git diff --staged`.
2. Group changes by stack (TS / Java / Python).
3. Run the relevant test command for each stack touched.
4. Write a conventional-commit message and commit. Do not push.
