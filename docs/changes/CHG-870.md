## CHG-870: Pre-existing E2E Failure — `us-730h-carrier-profile.spec.ts` "Saved" Label Never Appears

**Origin:** Discovered running the full `npm run test:e2e` suite as part of REVIEWER verification for the Admin persona sprint (US-874, 750-752, 875-878). Not caused by that work — `CarrierProfilePage.tsx`, its save flow, and `us-730h-carrier-profile.spec.ts` were not touched by any commit in this sprint.

**What was found:** `us-730h-carrier-profile.spec.ts`'s golden-path test ("fill all 4 tabs, save, reload, data persists") asserts `[data-testid="header-save-btn"]` contains "Saved" after clicking save. The button consistently shows "Save" (never transitions to "Saved"), reproduced both in the full suite (3-worker parallel run) and in isolation (single worker, run twice). Not a timing flake — deterministic failure both times.

**Impact:** Blocks a mechanical, no-exceptions reading of `docs/roles/REVIEWER.md`'s hard gate ("E2E Failure: `npm run test:e2e` has failures = REJECT") for any PR, regardless of relevance, until fixed.

**Decision (per Sequential Lock Protocol — escalate forward, don't block unrelated work):** Track this as a separate, pre-existing defect. The Admin persona sprint's REVIEWER sign-off proceeds on the full suite result: 129/130 unrelated-suite tests + all 5 new admin E2E tests passing, this one pre-existing failure carved out and referenced here. Someone must investigate whether `ProfileService.save()`'s success state (or the button's local "saved" flag) regressed, independent of this sprint.

**Not investigated further:** root cause of the label bug itself — out of scope for this sprint's CODER/REVIEWER pass.
